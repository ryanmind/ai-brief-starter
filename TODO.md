# TODO

## 黄仁勋风格流程升级（高吞吐 + 高可靠）

### P0（阻断发布，必须失败）
- [x] 无内容（最终条目数为 0）直接失败。
- [x] 关键结构损坏（JSON 解析失败、模板字段缺失）直接失败。
- [x] 关键可信度问题（缺来源链接、非一手来源比例异常）直接失败。

### P1（自动修复后继续）
- [x] 细节过短、与摘要重复：先 `--autofix`，再二次质检。
- [x] 输出夹杂英文：自动中文化清洗后再渲染。
- [x] 关键点数量/长度不达标：自动补齐或裁剪。

### P2（只告警不阻断）
- [x] 类别分布失衡（单类占比过高）仅告警。
- [x] 文风不一致、信息密度一般仅告警。
- [x] 非核心字段缺失（如影响字段为空）仅告警。

### 流程编排优化
- [x] 主链路改为：获取内容 -> 翻译内容 -> 模板成文 -> 自动修复质检 -> 二次质检 -> 发布。
- [x] 将“生成报告”与“飞书通知”解耦：通知失败不影响报告产出。
- [x] 将通知步骤改为 `continue-on-error` 或独立 workflow，并保留失败告警。
- [x] 当主链路失败时，仍需发送飞书失败摘要（失败步骤、错误原因、建议排查项、任务链接）。

### 可观测性与运营闭环
- [x] 每次运行输出指标：抓取数、过滤数、修复数、最终条数、失败原因 TopN。
- [x] 生成 `reports/quality_metrics.json` 供后续看板/趋势分析。
- [x] 增加“高风险条目清单”（融资/政策/数字密集），发布前人工 1 分钟复核。

---

更新日期：2026-03-03（全部条目已落地）
