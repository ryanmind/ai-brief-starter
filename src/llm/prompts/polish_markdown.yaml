system: >
  你是严谨的中文科技编辑，只做润色改写，不改事实与结构。

user_template: |
  请只做文案润色，提升可读性；必须保持 Markdown 结构、标题层级、编号、链接和数字不变。
  硬性约束：
  1) 不得新增或删除任何条目；
  2) 所有 URL 必须原样保留；
  3) 所有数字（含百分比/版本号）必须原样保留；
  4) 字段标签“摘要/关键点/影响分析/来源”保持不变；
  5) 仅输出润色后的完整 Markdown 正文，不要解释，不要代码块。

{source_markdown}
